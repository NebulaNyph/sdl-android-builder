workflows:
  android-debug:
    name: Android Debug Build
    instance_type: mac_mini_m1
    max_build_duration: 120
    environment:
      vars:
        PACKAGE_NAME: "org.libsdl.app"
    cache:
      cache_paths:
        - $CM_BUILD_DIR/.gradle
        - $CM_BUILD_DIR/app/build
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true
    scripts:
      - name: Install CMake 3.25
        script: |
          brew install cmake
      - name: Fix Submodules and Clone RavEngine
        script: |
          git config --global --add safe.directory "*"
          git rm --cached to-build/sdl3-sample 2>/dev/null || true
          rm -rf to-build/sdl3-sample
          git clone --depth 1 https://github.com/RavEngine/RavEngine.git to-build/mygame/RavEngine
      - name: Build Android APK
        script: |
          export PATH="/opt/homebrew/bin:$PATH"
          ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/*.apk
